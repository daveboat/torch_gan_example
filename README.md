A quick and easy-to-understand example of training a convolutional GAN on MNIST digits in pytorch.

```generator.pth``` is a pretrained model, run ```generate.py``` to use it to generate a single sample.